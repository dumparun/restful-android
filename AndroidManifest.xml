<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mn.aug.restfulandroid"
    android:versionCode="2"
    android:versionName="0.2" >

    <uses-sdk android:minSdkVersion="7" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".RestfulAndroid"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/RESTfulAndroidTheme" >
        <activity
            android:name=".activity.CatPicturesActivity"
            android:launchMode="singleTop" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.CommentActivity" />
        
        <service android:name=".service.DefaultCatPicturesService" />

        <provider
            android:name=".provider.CatPicturesProvider"
            android:authorities="mn.aug.restfulandroid.catpicturesprovider" />
    </application>

</manifest>